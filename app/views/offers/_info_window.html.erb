<h6><%= OFFERS["offer_types"][offer.contract_type] %></h6>
<hr class="my-2">
<p class="m-0"><i class="fas fa-user-md"></i> <%= offer.profession %></p>
<% if offer.type == "Replacement" %>
<p class="m-0">
    <i class="far fa-calendar-alt"></i>
    <% if offer.ends_at != nil %>
    du <%= l offer.starts_at, format: :default %> au <%= l offer.ends_at, format: :default  %>
    <% else %>
    à partir du <%= l offer.starts_at, format: :default %>
    <% end %>
</p>
<% end %>
<%= link_to "Voir l'annonce", offer_path(offer), class: "btn btn-primary btn-sm mt-2" %>