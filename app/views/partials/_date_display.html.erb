<% if Date.today == object.created_at.to_date %>
    <% if DateTime.now.hour == (object.created_at.hour + 2) %>
        <% if (DateTime.now.min - object.created_at.min).to_i == 0 %>
            à l'instant
        <% elsif (DateTime.now.min - object.created_at.min).to_i == 1 %>
            il y a <%= (DateTime.now.min - object.created_at.min).to_i %> minute
        <% else %>
            il y a <%= (DateTime.now.min - object.created_at.min).to_i %> minutes
        <% end %>
    <% else %>
        <% if (DateTime.now.hour - (object.created_at.hour + 2)).to_i == 1 %>
            il y a <%= (DateTime.now.hour - (object.created_at.hour + 2)).to_i %> heure
        <% else %>
            il y a <%= (DateTime.now.hour - (object.created_at.hour + 2)).to_i %> heures
        <% end %>
    <% end %>
<% elsif Date.today > object.created_at.to_date %>
    <% if (Date.today - object.created_at.to_date).to_i == 1 %>
        il y a <%= (Date.today - object.created_at.to_date).to_i %> jour
    <% else %>
        il y a <%= (Date.today - object.created_at.to_date).to_i %> jours
    <% end %>
<% end %>